import { ScrollView, StyleSheet, Text, View } from 'react-native'; import { useSafeAreaInsets } from 'react-native-safe-area-context'; import { Header, shared } from '../../src/components'; import { useLfg } from '../../src/lfg'; import { colors, radius, space } from '../../src/theme';
export default function Activity(){const insets=useSafeAreaInsets();const {boot}=useLfg();const findings=boot?.auto?.findings||[];return <View style={[shared.screen,{paddingTop:insets.top}]}><Header title="Activity" subtitle={`${findings.length} open findings`} /><ScrollView contentContainerStyle={shared.content}>{findings.map(f=><View key={f.id} style={s.card}><Text style={s.kind}>FINDING</Text><Text style={s.title}>{f.title}</Text>{f.summary?<Text style={s.body}>{f.summary}</Text>:null}{f.suggest?<Text style={s.suggest}>{f.suggest}</Text>:null}</View>)}{!findings.length?<Text style={s.empty}>Nothing needs attention.</Text>:null}</ScrollView></View>};const s=StyleSheet.create({card:{backgroundColor:colors.card,borderColor:colors.border,borderWidth:StyleSheet.hairlineWidth,borderRadius:radius.lg,padding:space.lg},kind:{color:colors.accent,fontSize:10,fontWeight:'900',letterSpacing:1.4},title:{color:colors.text,fontSize:17,fontWeight:'800',marginTop:5},body:{color:colors.textMuted,lineHeight:20,marginTop:space.sm},suggest:{color:colors.done,marginTop:space.sm},empty:{color:colors.textMuted,textAlign:'center',marginTop:80}});
